# Claude Code — session rules

The rules for this repository live in **AGENTS.md**, which every agent here
reads: Claude, Codex, Grok and Antigravity. One file, so a rule written once
is a rule all of them follow.

@AGENTS.md

If that import does not resolve, open `AGENTS.md` and read it before acting.
The core instructions are at `~/.claude/CLAUDE.md` and the standing rulebook at
`~/repos/RULES.md`.
